# Your own portal skills

`/add-portal` writes the job-portal search skills you generate here, one folder per portal (`<name>-search/`). `/scrape` picks them up automatically, alongside the portals shipped in the `ai-job-search-plugin` and `danish-job-portals` plugins.
